package sprout;

import java.util.Scanner;

public class spr_2475 {
	public static void main(String[] args) {
		Scanner input = new Scanner(System.in);
		System.out.println((int)((Math.pow(input.nextInt(), 2)+Math.pow(input.nextInt(), 2)+Math.pow(input.nextInt(), 2)+Math.pow(input.nextInt(), 2)+Math.pow(input.nextInt(), 2))%10));

	}
}
